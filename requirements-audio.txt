# Optional Audio Processing Libraries
# Install with: pip install -r requirements-audio.txt
# 
# These are optional dependencies for enhanced audio features
# but are included in main requirements.txt for convenience

# Audio analysis and feature extraction
librosa>=0.10.0

# Digital signal processing
scipy>=1.10.0

# Audio I/O (may require system dependencies)
# Windows: Use binary wheel or install Visual C++ Build Tools
# macOS: brew install portaudio libsndfile
# Linux: sudo apt-get install portaudio19-dev libsndfile1
pyaudio>=0.2.13

# Additional audio tools (optional)
# soundfile>=0.12.0  # For reading/writing audio files
# librosa-ml>=0.0.1  # ML extensions for librosa
# auralizer>=0.1.0   # For spatial audio

# These enable:
# - Real-time sound localization (Phase 3 feature)
# - Audio classification
# - Sound event detection
# - Obstacle detection via echoes
# - Frequency analysis
